#ifndef AUTH_H        // Include guard start: prevents multiple inclusion of this header
#define AUTH_H

/*
 * auth.h
 * Declaration for the administrator login function.
 */

/*
 * Attempt to authenticate the administrator.
 * Returns 1 for success, 0 for failure.
 */
int login(void);      // Function prototype for login authentication

#endif                // End of include guard
